import styled from '@emotion/styled';
import { Logo } from 'components/common/logo';

export const LogoHeaderDashboard = styled(Logo)`
  color: #626262;
  font-size: 3rem;
  filter: drop-shadow(2px 2px 5px #626262);
`;
